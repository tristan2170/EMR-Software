@model IEnumerable<Mock_EMR_Software.Models.Patient>

@{
    ViewBag.Title = "Index";
}

<br />
<h2 id="page-title">Patients</h2>

<div id="greeting">
<p>Welcome. Below are the patients in your system. Click "details" to discharge a patient, view comments, prescriptions and to add new ones</p>
</div>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
            Date Admitted
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.first_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.last_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_admitted)
        </td>
        <td>
            
            @Html.ActionLink("Details", "Details", new { id=item.patientId }) 
        </td>
    </tr>
}

</table>
<p>©Tristan Linnen</p>