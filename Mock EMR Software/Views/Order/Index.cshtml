@model IEnumerable<Mock_EMR_Software.Models.Orders>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.first_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prescriptions)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.first_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prescriptions)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.prescripId }) |
            @Html.ActionLink("Details", "Details", new { id=item.prescripId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.prescripId })
        </td>
    </tr>
}

</table>
